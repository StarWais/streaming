import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
  nostreams: {
    margin: '0',
    padding: '0',
    height: '96vh',
    maxWidth: '98vw',
    width: '100vw',
    overflow: 'hidden',
  },
  nostreamsText: {
    fontWeight: 'bold',
  },
});

export default styles;
