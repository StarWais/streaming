import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
  id: {
    fontWeight: 'bold',
    color: 'darkgray',
  },
  chatWrapper: {
    background: '#E9EFEF',
  },
});

export default styles;
