import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
  videopaper: {
    padding: '1rem',
    background: 'linear-gradient(to left,#7ED78F,#43C972)',
  },
});

export default styles;
